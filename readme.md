# 🏢 БТІ Фурси — Офіційний сайт та міні-CRM

Сучасний веб-додаток для **Бюро Технічної Інвентаризації**. Складається з публічного сайту та захищеної панелі адміністратора для керування контентом.

---

## 🌟 Основні можливості

### 🖥️ Клієнтський сайт
- ✅ **Динамічний контент** — новини, FAQ, послуги, контакти з бази даних
- ✅ **Адаптивний дизайн** — оптимізація для мобільних та ПК
- ✅ **SEO-дружелюбність** — динамічний sitemap.xml, мета-теги, Open Graph
- ✅ **Оптимізація зображень** — автоматична конвертація в WebP
- ✅ **Форма заявок** — з сповіщеннями у Telegram
- ✅ **Завантаження документів** — PDF, DOC та інші формати

### 🔧 Панель адміністратора (Міні-CRM)
- ✅ **Керування контентом** — редагування новин, FAQ, цін на послуги
- ✅ **Обробка заявок** — статуси: Нова → В роботі → Завершено
- ✅ **Керування файлами** — безпечне завантаження та стиснення
- ✅ **Безпека** — JWT токени, bcrypt шифрування, захист від XSS

---

## 🛠️ Технологічний стек

| Компонент | Технологія |
|-----------|-----------|
| **Backend** | Python 3.10+ • FastAPI • SQLAlchemy |
| **База даних** | SQLite3 (`bti.db`) |
| **Безпека** | JWT (PyJWT) • bcrypt |
| **Обробка даних** | Pillow (зображення) • python-multipart |
| **Frontend** | HTML5 • CSS3 • Vanilla JavaScript |

### 📚 Залежності Backend
```
FastAPI, uvicorn, SQLAlchemy, PyJWT, bcrypt
python-dotenv, Pillow, python-multipart
```

---

## 🚀 Встановлення та запуск

### Step 1️⃣ Backend (FastAPI)

```bash
cd backend
python -m venv venv

# На Windows:
venv\Scripts\activate
# На Mac/Linux:
source venv/bin/activate

pip install fastapi uvicorn sqlalchemy pyjwt bcrypt python-dotenv pillow python-multipart
```

Запуск сервера:
```bash
uvicorn main:app --reload
```
✅ Backend готовий: `http://127.0.0.1:8000`

---

### Step 2️⃣ Frontend (HTML/JS)

```bash
cd ../frontend
python -m http.server 5500
```
✅ Сайт готовий: `http://localhost:5500`

---

### 🔑 Налаштування `.env`

Создайте файл `backend/.env`:

```env
SECRET_KEY=your_super_secret_key_here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin2026
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

---

## 📂 Структура проєкту

```
bti_proj/
│
├── 📁 backend/
│   ├── main.py              # Основний FastAPI код (routers, моделі, логіка)
│   ├── bti.db               # SQLite база (створюється автоматично)
│   ├── uploads/             # Папка для фото та документів
│   ├── venv/                # Віртуальне середовище
│   └── .env                 # Змінні середовища (не в Git!)
│
├── 📁 frontend/
│   ├── 📄 index.html        # Головна сторінка
│   ├── 📄 article.html      # Шаблон для статей
│   ├── 📄 news.html         # Сторінка з усіма новинами
│   ├── 📄 style.css         # Основні стилі
│   ├── 📄 config.js         # API конфігурація
│   ├── 📄 contacts.js       # Завантаження контактів
│   ├── 📁 img/              # Лого, іконки, картинки
│   │
│   └── 📁 admin/            # Панель керування
│       ├── index.html       # Інтерфейс адмінки
│       ├── admin.js         # Логіка адмінки
│       └── admin-style.css  # Стилі адмінки
│
└── 📄 README.md             # Цей файл
```

---

## 🌐 Розгортання на Production (VPS)

### Необхідні кроки:

1. **Веб-сервер**
   - Встановити **Nginx** для роздачі статики (`frontend` папка)
   - Налаштувати Reverse Proxy на порт `:8000` для API

2. **Фоновий процес**
   - Запустити FastAPI через `systemd` або `supervisor`
   - Забезпечити автоперезавантаження при перезавантаженні système

3. **SSL-сертифікат**
   - Встановити **Let's Encrypt** для HTTPS
   - Налаштувати автоматичне оновлення сертифіката

4. **Безпека (backend/main.py)**
   ```python
   # Замінити:
   allow_origins=["*"]
   # На:
   allow_origins=["https://your-domain.ua"]
   ```

---

## 📋 Контрольний список перед запуском

- [ ] Створена папка `backend/.env` з усіма змінними
- [ ] Встановлені всі залежності (`pip install -r requirements.txt`)
- [ ] Backend запущений на порту `:8000`
- [ ] Frontend запущений на порту `:5500`
- [ ] Бази даних `bti.db` створена автоматично
- [ ] Папка `uploads/` має права на запис
